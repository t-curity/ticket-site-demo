name: build-push-deploy-frontend

on:
  push:
    branches: ["develop"]

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/t-curity/ticket-site-demo

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE }}:sha-${{ github.sha }}
            ${{ env.IMAGE }}:develop

      - name: Trigger infra-deploy (general/frontend)
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.INFRA_DEPLOY_TOKEN }}
          repository: t-curity/infra-deploy
          event-type: deploy
          client-payload: |
            {"target":"general","service":"frontend","tag":"sha-${{ github.sha }}"}
